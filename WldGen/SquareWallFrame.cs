using NeoDraw.Undo;
using Terraria;

namespace NeoDraw.WldGen {

    public partial class WldGen {

		/*public static void SquareWallFrame(int i, int j, ref UndoStep undo, bool resetFrame = true) {

			WallFrame(i - 1, j - 1, ref undo);
			WallFrame(i - 1, j, ref undo);
			WallFrame(i - 1, j + 1, ref undo);
			WallFrame(i, j - 1, ref undo);
			WallFrame(i, j, ref undo, resetFrame);
			WallFrame(i, j + 1, ref undo);
			WallFrame(i + 1, j - 1, ref undo);
			WallFrame(i + 1, j, ref undo);
			WallFrame(i + 1, j + 1, ref undo);

		}
		*/

		/*public static void SquareWallFrame(int i, int j, bool resetFrame = true) {

			WallFrame(i - 1, j - 1);
			WallFrame(i - 1, j);
			WallFrame(i - 1, j + 1);
			WallFrame(i, j - 1);
			WallFrame(i, j, resetFrame);
			WallFrame(i, j + 1);
			WallFrame(i + 1, j - 1);
			WallFrame(i + 1, j);
			WallFrame(i + 1, j + 1);

		}
		*/

	}

}
